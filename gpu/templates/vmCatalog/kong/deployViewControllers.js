'use strict';

angular.module('gpu.controllers')
   .controller('kongDeployViewCtrl', function ($scope, $location, $state, $stateParams,$mdDialog, $q, $filter, $timeout, $interval, user,paging, common, ValidationService, vmCatalogService, CONSTANTS) {
        _DebugConsoleLog("kong deployViewControllers.js : kongDeployViewCtrl", 1);
       var ct = $scope.$parent.$parent.contents;

    })
;
