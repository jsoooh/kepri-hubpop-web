'use strict';

angular.module('gpu.controllers')
   .controller('mongodbDeployViewCtrl', function ($scope, $location, $state, $stateParams,$mdDialog, $q, $filter, $timeout, $interval, user,paging, common, ValidationService, vmCatalogService, CONSTANTS) {
        _DebugConsoleLog("mongodb deployViewControllers.js : mongodbDeployViewCtrl", 1);
       var subPage = this;
       var ct = $scope.$parent.$parent.contents;

    })
;
